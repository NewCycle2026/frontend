/**
 * 🌍 국가별 위험등급 관리
 * - 각 국가별로 위험 등급 수동/자동 설정 가능
 * - 사용자 여행 제한 여부 적용
 */

export default function RiskCountries() {
  return (
    <div>
      <h2>위험 국가 설정</h2>
      <p>국가별 여행 위험 등급 등록 및 업데이트</p>
    </div>
  );
}
